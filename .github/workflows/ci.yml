# =============================================================================
# CI de garde (CC-149) — rejoue les gates du dépôt à chaque PR vers master.
#
# ⚠️ **Les gates elles-mêmes ne sont plus ici** (CC-142) : elles vivent dans
# `gates.yml`, appelé ci-dessous, parce que `release.yml` doit les rejouer avant de
# publier une image et qu'une recopie aurait divergé en silence. Ce fichier ne
# répond plus qu'à une question : **quand** les jouer. Ce qu'elles prouvent — et
# surtout ce qu'elles n'attrapent pas — est documenté en tête de `gates.yml`, et
# c'est là qu'il faut lire avant de se fier à un vert.
#
# Deux bénéfices, et le second est celui qui compte le plus ici.
#
#   1. Les gates ont réellement tourné. Le verdict s'affiche sur la PR, lisible par
#      quiconque, sans dépendre de la bonne foi ni de la mémoire de qui l'a ouverte.
#   2. La suite ne dépend pas du poste. Il n'existe AUCUN `.env` sur un runner, donc
#      la classe de bug mesurée en CC-88 — une valeur vide dans `.env.test` qui ne
#      masque rien et laisse passer le `.env` réel de la machine, parce que le
#      chargeur d'@adonisjs/env fusionne sur la *truthiness* — y est structurellement
#      impossible. C'est le seul environnement dont on puisse le dire.
# =============================================================================
name: CI

on:
  # `push` sur master seul, et `pull_request` vers master : déclencher `push` sur toutes
  # les branches ferait tourner le job DEUX fois par PR (une fois par événement), pour
  # deux verdicts identiques.
  #
  # ⚠️ Conséquence à connaître : sur une branche de travail, le SEUL signal est
  # l'événement `pull_request`. Un workflow ajouté par une PR s'y applique bien — GitHub
  # prend le fichier depuis la ref de merge, pas depuis master.
  push:
    branches: [master]
  pull_request:
    branches: [master]

# Ce workflow ne publie rien et n'écrit nulle part : son jeton n'a que la lecture.
permissions:
  contents: read

# Une PR mise à jour annule le run devenu obsolète — il portait sur des commits que
# personne ne relira. Le groupe reste constant par PR (`github.ref` ne change pas d'un
# push à l'autre sur la même PR), donc chaque nouveau push y retrouve le précédent et
# l'annule.
#
# ⚠️ **CC-160** : `cancel-in-progress: false` seul ne suffit PAS à protéger master — il
# ne protège que le run EN COURS. GitHub ne garde qu'UN SEUL run en attente par groupe :
# un troisième push rapproché annule le deuxième (celui qui attendait), même avec ce
# réglage. Constaté le 2026-08-04 : trois merges coup sur coup ont laissé le run du
# milieu annulé — gris, jamais rouge, verdict jamais rendu. La protection vient donc du
# groupe lui-même : sur `push` (restreint à master par le déclencheur ci-dessus), le
# groupe est unique par SHA. Deux pushes ne partagent alors jamais de groupe, donc aucun
# ne peut jamais mettre l'autre en file ni l'annuler.
concurrency:
  group: ci-${{ github.event_name == 'pull_request' && github.ref || github.sha }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # ⚠️ Le chemin est relatif à la RACINE du dépôt (`./.github/…`), jamais au dossier
  # courant du fichier : un `uses: ./gates.yml` échoue au déclenchement, pas à l'écriture.
  gates:
    uses: ./.github/workflows/gates.yml
    permissions:
      contents: read
